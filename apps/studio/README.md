# SolaceFrame Studio V17

V17 is the storage-backed execution artifact delta.

The studio can execute an admissible render job through Vercel AI Gateway, persist generated media into Supabase Storage, and replay the resulting durable artifact URL in the interface.

Run the V17 migration before deployment.
